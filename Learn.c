// #include <stdio.h>
// int main()
// {
   /* num = int(input("enter the no. upto which you wish to print the fibonacci series: "))
    x,y = 0,1
    print(x)
    while y<=num:
    print(y)
    x,y = y,x+y
    */
   // int x=0,y=1,num,temp;
   // printf("Enter a number : ");
   // scanf("%d",&num);
   // printf("\nFibonacci Series up to %d:\n",num);
   // printf("%d\n",x);
   // while (y<=num)
   // {
   //  printf("%d\n",y);
   //  temp = x;
   //  x = y;
   //  y = temp + y;
   // }
   /*hello this is the temp code ::*/
   // int i;
   // int marks[5]={1,2,2,3,5};
   // int size = sizeof(marks)/sizeof(marks[0]);
   // printf("[");
   // for(i=0;i<=size;i++){
   //    printf("%d,",marks[i]);
   // }
   // printf("]");
   
//    return 0;
   
// }
#include <stdio.h>
int main(){
   int** threeSum(int* nums, int numsSize, int* returnSize, int** returnColumnSizes) {
    int ls = [];
    int len = sizeof(nums);
    return len;
   }
   int nums = [-1,0,1,2,-1,-4];
   [[-1,1,0],[2,-1,-1]]
}